rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      // Allow read and write for user profiles
      allow read, write: if true;
    }
  }
}
